[email]
    # 是否启用邮件通知
    enabled = true
    # SMTP 服务器
    smtp_host = "smtp.qq.com"
    smtp_port = 587
    # 发件人邮箱
    username = "3207893944@qq.com"
    # SMTP 授权码 (非邮箱密码, 在邮箱设置中生成)
    password = "iquhqfgvrjzmdeci"
    # 收件人
    to = "wrl0512@163.com"
    # 任务名 (用于邮件标题)
    task_name = "TileClaw Download"

[alert]
    # 错误阈值: 冷却窗口内超过此数量发告警
    error_threshold = 50
    # 冷却时间 (分钟), 同类型告警不重复发送
    cooldown_minutes = 30
    # 统计窗口 (分钟), 在此时间内的错误计入阈值
    window_minutes = 10
